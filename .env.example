# No runtime environment variables are required for the public landing site.
# VITE_GA_DEBUG=1 loads Google Analytics on any host (localhost, a Vercel
# preview) after Accept, with debug_mode on so events show in DebugView.
# Leave it unset in production: GA then loads on the tripcerto.com hosts only.
VITE_GA_DEBUG=
